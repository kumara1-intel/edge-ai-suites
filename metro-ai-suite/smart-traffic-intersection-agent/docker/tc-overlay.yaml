# Copyright (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
#
# Trusted Compute overlay - included automatically when deploying with env ENABLE_TRUSTED_COMPUTE=true

services:
  dlstreamer-pipeline-server:
    runtime: io.containerd.kata.v2
    privileged: false
    volumes:
      - dlstreamer-pipeline-server-pipeline-root:/var/cache/pipeline_root
      - ./tc-resolv.conf:/etc/resolv.conf:ro
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 4G
        reservations:
          cpus: '2'
          memory: 2G

  vlm-openvino-serving:
    runtime: io.containerd.kata.v2
    healthcheck:
      start_period: 300s
    volumes:
      - ./tc-resolv.conf:/etc/resolv.conf:ro
    deploy:
      resources:
        limits:
          cpus: '6'
          memory: 10G
        reservations:
          cpus: '4'
          memory: 8G

  kata-dns-relay:
    image: coredns/coredns:1.14.2
    networks:
      scenescape:
        ipv4_address: 172.20.0.200
    configs:
      - source: corefile
        target: /Corefile
    command: -conf /Corefile
    restart: on-failure:5

volumes:
  dlstreamer-pipeline-server-pipeline-root:
    driver_opts:
      o: uid=1999,gid=1999

networks:
  scenescape:
    ipam:
      config:
        - subnet: 172.20.0.0/16

configs:
  corefile:
    content: |
      . {
          forward . 127.0.0.11
          errors
          #log
      }
